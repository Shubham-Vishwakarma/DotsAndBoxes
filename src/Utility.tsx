class Utility {

    static min(a: number, b: number): number {
        return a < b ? a : b;
    }

    static max(a: number, b: number): number {
        return a > b ? a : b;
    }

}

export default Utility;